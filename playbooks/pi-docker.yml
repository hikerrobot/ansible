- hosts: "{{ host_ip }}"
  remote_user: rob
  become_user: root
  become: true
  vars:
    pip_package: python3-pip
    docker_version: latest
    docker_install_recommends: true
    docker_users:
      - pi
  roles:
    - geerlingguy.pip
    - geerlingguy.docker_arm
